import 'package:flutter/material.dart';

class AppColors {
  // Solo Leveling Inspired Palette
  static const Color background = Color(0xFF050505); // Deep Black
  static const Color surface = Color(0xFF121212);    // Dark Grey
  static const Color primary = Color(0xFF00A8FF);    // System Blue (Glowing)
  static const Color secondary = Color(0xFF00FFFF);  // Cyan Accent
  static const Color accent = Color(0xFF4B0082);     // Shadow Purple
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color error = Color(0xFFFF3333);
  
  // Legacy mappings (to keep existing code working until fully refactored)
  static const Color neutral = Color(0xFF808080);   // Grey for disabled/neutral
}
